<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/ft_toolbar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorPrimary"
    android:minHeight="?attr/actionBarSize"
    android:fitsSystemWindows="true"
    app:popupTheme="@style/Theme.AppCompat.Light">

    <RadioGroup
        android:id="@+id/rg_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/rb_local_music"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/selector_btn_music_fg"
            android:textColor="@drawable/selector_text_color"
            android:textSize="16dp"
            android:button="@null"
            android:checked="true"
            android:text="@string/string_my_music"/>

        <RadioButton
            android:id="@+id/rb_online_music"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/selector_btn_music_fg"
            android:textColor="@drawable/selector_text_color"
            android:textSize="16dp"
            android:button="@null"
            android:text="@string/string_online_music"/>
    </RadioGroup>
</android.support.v7.widget.Toolbar>